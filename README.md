# CareNote AI

AI-assisted discharge documentation MVP using RAG, Groq, FAISS, Hugging Face embeddings, Streamlit, PDF/DOCX export, and optional Google OIDC authentication.

## Deployment
Deploy `app.py` on Streamlit Community Cloud. Add Groq and Google OIDC secrets in Streamlit Cloud; never commit `.streamlit/secrets.toml`.
